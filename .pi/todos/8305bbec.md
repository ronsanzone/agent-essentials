{
  "id": "8305bbec",
  "title": "Restructure claude-essentials for npx skills and safe Claude config install",
  "tags": [
    "claude-essentials",
    "migration"
  ],
  "status": "closed",
  "created_at": "2026-06-27T15:05:59.086Z"
}

Implemented migration:
- Materialized ~/.claude/.mcp.json and ~/.claude/settings.local.json as real local files with backups before removing repo-owned symlinks.
- Moved canonical skills from .claude/skills to top-level skills/.
- Moved safe Claude config into config/claude/ and added sanitized mcp.json.example.
- Added scripts/install-claude.sh with migration cleanup, safe config install, examples, and npx skills install.
- Converted scripts/link-claude-files.sh into a compatibility shim.
- Rewrote README.md and INSTALL.md around npx skills and safe config install.
- Updated .gitignore to ignore generated/local .claude state and local config secrets.
- Updated ~/code/dot profile to run scripts/install-claude.sh and extended dot doctor --deep to check ~/.claude symlinks.
- Ran installer, installed 14 skills into ~/.claude/skills, verified npx skills discovery, verified ~/.claude/.mcp.json and settings.local.json are real files, and verified no broken ~/.claude symlinks.

Note: dot doctor --deep still fails due pre-existing broken symlinks under ~/.config and ~/.pi/agent, not due ~/.claude.
